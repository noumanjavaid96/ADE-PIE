{
  "name": "confidence-score-mockup",
  "version": "1.0.0",
  "description": "An interactive UI mock-up to demonstrate the Confidence Score logic.",
  "main": "index.html",
  "scripts": {
    "start": "http-server -c-1 -o",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jules",
  "license": "UNLICENSED",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}